# Source archives (superseded by the working tree; kept on disk, not in git)
*.zip

# Secrets / env — never committed
.env
.env.*
**/.env

# Supabase CLI local state
supabase/.temp/
supabase/.branches/

# OS / editor noise
.DS_Store
Thumbs.db
node_modules/

# Machine-local Claude Code settings
.claude/settings.local.json
deno.lock

# Playwright local artifacts + sandbox-only config override (never committed)
test-results/
playwright-report/
pw-sandbox.config.ts
